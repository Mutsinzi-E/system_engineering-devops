#!/usr/bin/env bash
# Script to kill the 4-toinfinityand_beyond process safely

pid=$(pgrep -f 4-toinfinityand_beyond)
if [ -n "$pid" ]; then
    kill "$pid"
else
    echo "No 4-toinfinityand_beyond process found."
fi
